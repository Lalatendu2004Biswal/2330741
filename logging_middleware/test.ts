import { Log } from "./index";

// Test it
Log("backend", "info", "middleware", "Logging middleware initialized successfully");
Log("backend", "error", "db", "Test database connection error");
Log("backend", "debug", "config", "Config loaded for testing");
